{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Ajouter des Contacts</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('contact') }}">
            Retourner à la liste
        </a>
    </li>
</ul>
{% endblock %}
